---
# is this needed?
apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: frontend
  namespace: {{ app_name }}
spec:
  hosts:
    - frontend
  http:
    - route:
        - destination:
            host: frontend
            port:
              number: 80
